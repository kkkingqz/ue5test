---
title: Authoring Index
status: informative
version: 1.0
updated: 2026-08-16
depends_on:
  - ../README.md
---

# Наполнение игры контентом

Этот раздел — для того, кто наполняет игру: придумывает предметы, персонажей, локации и тексты. Программирование не требуется. Всё, что описано здесь, делается в текстовых файлах и проверяется одной командой.

Остальные разделы документации написаны для программистов; сюда заглядывать за ними не нужно.

## С чего начать

| Документ | О чём |
|---|---|
| [Setup](Setup.md) | Где лежит контент, какие команды запускать, как выглядит рабочий цикл |
| [Objects](Objects.md) | Какие бывают объекты, какие у них поля и как они связаны между собой |
| [Characters](Characters.md) | Как описать персонажа — от текста имени до проверки |
| [Texts](Texts.md) | Тексты и переводы |
| [Events](Events.md) | Что такое событие, какие есть и что можно сделать самому |

Если вы здесь впервые — читайте [Setup](Setup.md), потом [Characters](Characters.md): на примере персонажа видно весь путь целиком.

## Два уровня: `core` и `rh`

Контент лежит в двух пакетах, и это разделение важно понимать с самого начала.

**`GameData/rh`** — ваша игра. Сюда идут предметные схемы (предметы, локации, расширения актора), конкретные предметы, персонажи, локации и их тексты. Здесь вы работаете каждый день.

**`GameData/core`** — движок. Здесь описаны общие механизмы и framework-схемы (экраны, тексты, ресурсы, базовый актор). Правки в `core` — задача программиста.

Простое правило: **игра и её предметная модель — в `rh`, общие механизмы — в `core`.** Добавить меч, торговца, таверну — `rh`, вы сами. Сделать так, чтобы появился новый тип интерфейсного экрана или новый рантайм-сервис — `core`, через программиста.

## Что вы можете сами

- Добавлять предметы, персонажей и локации.
- Писать все тексты и переводы.
- Менять цены, характеристики, состав локаций.
- Проверять свою работу и видеть ошибки с указанием файла и строки.

## Что потребует программиста

- Новое поле у игрового объекта (например, вес предмета) — меняется схема в `rh`.
- Новый вид игрового объекта (например, «заклинание») — новая схема в `rh`.
- Новый экран или новый элемент интерфейса — `core`.
- Новое действие игрока (команда) и реакция на событие.

Это не «сложно» и не «долго» — просто делается в другом месте. Формулируйте задачу как «нужно, чтобы у предмета было поле X» — этого достаточно.

---

**Раздел не является нормативным.** Это инструкции, а не правила системы. Точные правила живут в архитектурных контрактах (`Docs/Architecture/`); если инструкция разойдётся с ними, права сторона контракта. Для работы с контентом это различие обычно не имеет значения.
