---
title: Test Tiers Tasks
status: draft
version: 1.0
updated: 2026-08-18
depends_on:
  - PackageSet.md
  - ../../Architecture/HeadlessSimulationContract.md
---

# M4 — Test Tiers

> **Материализует:** [Headless Simulation Contract](../../Architecture/HeadlessSimulationContract.md) в части конфигураций сессий для спеков.
> **Задачи:** TSL-15…17.
> **Результат:** уровень проверяется без слоёв выше него.

## Результат этапа

Спека ядра не может незаметно опереться на `textsystem`, а спека `textsystem` — на `rh`. Сегодня это возможно: всё, кроме `Tests/Lua/commands`, идёт на одной сессии с полным набором пакетов.

## Задачи

- [ ] **TSL-15 — Карта подкаталога в конфигурацию пакетов**
  - Зависимости: TSL-05.
  - Разбиение сегодня бинарно: фикстурная сессия задана литералом `{"commands"}` в `LuaSpecRunner.cpp`, остальное идёт на боевой сессии.
  - Done: подкаталог `Tests/Lua/` объявляет требуемую конфигурацию пакетов; конфигураций три — ядро, ядро плюс `textsystem` с образцом, полный игровой набор; карта живёт в одном месте и используется обоими хостами; подкаталог без объявленной конфигурации отвергается, а не попадает на боевую сессию по умолчанию.
  - Evidence: <!-- tests/commit/PR -->

- [ ] **TSL-16 — Расширить пакет-образец**
  - Зависимости: TSL-15.
  - Отдельный `sample_text_game` не заводится: два демонстрационных пакета разойдутся.
  - Done: существующий `sample` получает актора, три локации со связностью и экран, достаточные для проверки `textsystem` без `rh`; `sample` по-прежнему не входит в игровой набор по умолчанию и подключается конфигурацией сессии.
  - Evidence: <!-- tests/commit/PR -->

- [ ] **TSL-17 — Разнести существующие спеки**
  - Зависимости: TSL-16.
  - Done: `world`, `economy`, `actions`, `presentation`, `authoring` и прочие подкаталоги отнесены к уровням; спеки, проверяющие правила `rh`, не выполняются на конфигурации без `rh`; спека, случайно опирающаяся на слой выше своего уровня, падает — проверено переносом одной спеки на нижнюю конфигурацию в рамках задачи; contract описывает три конфигурации и правило отнесения.
  - Evidence: <!-- tests/commit/PR -->

## Проверка milestone

- [ ] Спеки ядра проходят без `textsystem` и `rh`.
- [ ] Спеки `textsystem` проходят без `rh`.
- [ ] Подкаталог без объявленной конфигурации отвергается.
- [ ] Опора на слой выше своего уровня обнаруживается падением.
