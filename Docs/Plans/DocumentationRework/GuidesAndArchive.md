---
title: Guides and Archive Tasks
status: active
version: 1.3
updated: 2026-08-20
depends_on:
  - AuthoringDocs.md
  - ../../Guides/README.md
  - ../../Status/ImplementationStatus.md
---

# M5 — Guides and Archive

> **Материализует:** решения о разделении инструкций по аудитории, инверсии статуса и сжатии архива.
> **Задачи:** DOC-12…16.
> **Результат:** `Guides/` описывает работу с движком, статус перечисляет пробелы, архив перестаёт занимать треть корпуса.

## Результат этапа

Инструкции разделены по аудитории: дизайнер читает `Authoring/`, программист — `Guides/`. Оба каталога описывают то, что действительно делают.

## Задачи

- [ ] **DOC-12 — Разделить `Guides/` по аудитории**
  - Зависимости: DOC-11.
  - Done: `AddActorWrapper.md` **удалён** — его предмет не существует: механизм `register_type` с ручным `setmetatable`-декоратором убран решением по авторским расширениям сущностей; `AddCommand`, `AddEvent`, `AddDefinition` и `AddUIScreen` переработаны как задачи дизайнера и перенесены в `Docs/Authoring/`; `AddLuaModule` и `AddLuaSpec` остаются в `Guides/` и переработаны — второй обязан знать о трёх тирах спек, появившихся после его последней правки; все ссылки обновлены.
  - Evidence: `Docs/Guides/`, `Docs/Authoring/`.

- [ ] **DOC-13 — Недостающие гайды программиста**
  - Зависимости: DOC-12.
  - Каждый гайд отвечает реальной повторяющейся задаче этого репозитория, а не гипотетической.
  - Done: добавлены только четыре гайда с подтверждёнными повторяющимися потребителями:
    - **AddAuthoringSurface** — новый глобал в `_ENV`, прокси с `__newindex`, отложенная регистрация, заморозка, атрибуция ошибок пакету; самая частая задача последних планов;
    - **AddScreenField** — схема поля, адаптер и `WBP_*` через `unreal-mcp`;
    - **RegenerateGolden** — какой хэш от чего меняется, воспроизведение манифестом, кэширование хэша на конфигурации CMake;
    - **WhenToWriteCpp** — критерий [ADR-0020](../../ADR/0020-cpp-scope-criterion.md) как процедура, а не как принцип.
  - `AddRegistry` не создаётся: новый registry является архитектурным решением, а общий protocol принадлежит `RuntimeFacadeAndRegistries.md`, не инструкции по копированию. `AddGate` не создаётся до появления минимум двух независимых повторяющихся consumers; обязательные требования к gate остаются в `BuildAndTooling.md` и рядом с существующими test helpers.
  - Для каждого нового гайда в change set указаны минимум два фактических пути/изменения, подтверждающих повторяемость задачи; без такого evidence документ не создаётся.
  - Каждый гайд явно называет свой предмет в шапке, чтобы правило DOC-04 было проверяемым.
  - Evidence: `Docs/Guides/`.

- [ ] **DOC-14 — Инвертировать `ImplementationStatus`**
  - Зависимости: DOC-12.
  - Документ перечисляет реализованное и обязан обновляться при каждом расширении объёма — то есть структурно обречён расходиться с кодом.
  - Done: документ перечисляет только **подтверждённые незакрытые расхождения** нормативных contracts с реализацией: `missing`, `partial` или `known_nonconformance`; каждая строка содержит ссылку на конкретное нормативное правило, точное расхождение и evidence из кода/test либо отсутствие требуемого evidence; roadmap, приоритеты, предложения и «ближайшие возможности» запрещены, потому что принадлежат Plans/Proposals; строки «реализовано» удалены — на этот вопрос отвечают код и tests; одновременное добавление contract и полностью соответствующей реализации не требует новой строки, закрытие существующего gap удаляет её в том же change set; правило обновления в `AGENTS.md` приведено в соответствие.
  - Evidence: `Docs/Status/ImplementationStatus.md`, `AGENTS.md`.

- [ ] **DOC-15 — Перевести архив планов в плоские summary-файлы**
  - Зависимости: DOC-14.
  - Архив — 102 файла и 54 тысячи слов при 23 файлах активных планов. После выполнения для оперативного чтения нужны итог каждого этапа, task IDs и маршрут к полной истории; сами task-файлы остаются доступными в git.
  - Формат: выполненный `<PlanName>` представлен одним `Docs/Plans/Archive/<PlanName>.md` со `status: archived`. Summary содержит цель и общий результат; раздел по каждому milestone/этапу с кратким итогом; **все** шаги этого этапа в форме `<task ID> — <исходное название>` (`SAV-03 — ...`, `SAV-04 — ...`) и в исходном порядке; ссылки на актуальные normative contracts; полный hash `source_commit` и кликабельную ссылку на этот commit в repository web UI, где доступны полные файлы плана. Task ID обязан встретиться ровно один раз и не меняется при архивации.
  - Процедура для нового выполненного плана: все checkboxes и milestones сначала фиксируются отдельным commit при ещё существующем полном каталоге плана; этот commit становится `source_commit`; затем создаётся flat summary, обновляется `Archive/README.md`, план удаляется из списка активных, исходный `Docs/Plans/<PlanName>/` удаляется, и выполняется archive commit. Summary не может ссылаться на archive commit как на источник полного плана — это была бы circular-ссылка.
  - Миграция существующего архива: до первой правки фиксируется HEAD с полными `Docs/Plans/Archive/<PlanName>/`; для каждого каталога создаётся соседний `<PlanName>.md`, после чего каталог удаляется. Все входящие ссылки из-за пределов удаляемых каталогов инвентаризируются и перенаправляются на flat summary либо на нормативный owner contract.
  - `Docs/Plans/README.md`: раздел с таблицей выполненных планов удалён полностью; вместо него остаётся только ссылка `[Архив выполненных планов](../Archive/README.md)`. Перечень, дата завершения и краткий результат принадлежат только `Docs/Plans/Archive/README.md`, который ссылается на flat summary-файлы.
  - Verification: для каждого удаляемого файла выполняется `git cat-file -e <source_commit>:<original_path>`; ссылка на commit проверяется; для каждого summary автоматически либо эквивалентной проверкой сопоставляются milestone names и полный набор task IDs исходного плана; минимум один representative файл восстанавливается через `git show`; после удаления не остаётся подкаталогов планов внутри `Docs/Plans/Archive/`; **zip не создаётся**.
  - Правило архивации в `Docs/Plans/README.md` и `AGENTS.md` описывает эту же двухкоммитную процедуру, обязательную полноту task IDs и плоскую структуру без альтернативного варианта.
  - Evidence: `Docs/Plans/Archive/`, `Docs/Plans/README.md`, `AGENTS.md`.

- [ ] **DOC-16 — Сквозная верификация**
  - Зависимости: DOC-15, DOC-06, DOC-09.
  - Done: `validate_docs.py`, его positive/negative legacy cases и CTest зелёные; поскольку задачи меняют Python/CMake tooling, выполнены обязательные repository checks для code changes; документов-сирот нет; циклов зависимостей и битых anchors нет; каждый гайд и каждый документ `Authoring/` сверен с фактическим кодом; корпус повторно измерен относительно DOC-00 и изменение объёма зафиксировано в отчёте; правила `AGENTS.md` перечитаны на противоречия после всех правок; восстановление архива из `source_commit` проверено.
  - Evidence: отчёт validator-а, отчёт CTest, результат обязательных code checks, archive restore report.

## Проверка milestone

- [ ] `Guides/` содержит только задачи программиста, каждая — с явно названным предметом.
- [ ] Инструкции по удалённым механизмам отсутствуют.
- [ ] `ImplementationStatus` не требует правки при добавлении возможности.
- [ ] `ImplementationStatus` не содержит roadmap или backlog и каждая строка ссылается на нормативное правило.
- [ ] `Docs/Plans/README.md` не содержит перечня выполненных планов — только ссылку на `Archive/README.md`.
- [ ] `Docs/Plans/Archive/` не содержит подкаталогов планов; один выполненный plan соответствует одному `<PlanName>.md`.
- [ ] Каждый summary содержит результат всех этапов, полный набор task IDs и кликабельную ссылку на заранее зафиксированный `source_commit`.
- [ ] Каждый удалённый path существует в `source_commit` и проверяемо восстанавливается; zip не создан.
