---
title: Acceptance Evidence Portability Implementation Plan
status: active
version: 0.6
updated: 2026-09-20
depends_on:
  - ../../Architecture/BuildAndTooling.md
  - ../../Proposals/ContainerizedExecutionEnvironmentProposal.md
decisions:
  - ../../ADR/0043-presentation-apply-boundary.md
---

# Acceptance Evidence Portability: план реализации

> **Материализует:** решение `B` из [ContainerizedExecutionEnvironmentProposal](../../Proposals/ContainerizedExecutionEnvironmentProposal.md) — приёмка GV2 исполняется именованным серверным шаблоном, а не локальным раннером.
> **Не является нормативным:** нормативная форма запуска приёмки фиксируется задачей `AEP-05` до того, как на неё начнут ссылаться.
> **Исполнение:** последовательно по задачам. Checkbox задачи — единственный источник её завершения.

**Цель:** сделать приёмочное evidence переносимым — чтобы прогон исполнял кто угодно, а доказательная сила проверки от этого не зависела. Сегодня она зависит: producer и consumer совпадают потому, что это один процесс.

**Технологии:** существующие `Tools/Testing/run_ue_acceptance.py` и `ue_test_report.py`, CTest-гейты, UE automation. Нового runtime-модуля план не требует.

## Измеренное состояние

**Валидация уже отделима, но не отделена.** `validate_run(discovered, report, run_identity)` — чистая функция: множество обнаруженных тестов, отчёт и ожидаемая идентичность. Producer этих трёх входов — `run_acceptance()`, и он же consumer, в одном процессе. Ничто, кроме этого совпадения, не связывает evidence с тем, что его породило.

**Идентичность вычисляется локально.** `compute_run_identity` собирает `run_id`, `source_revision`, `source_diff_hash`, `build_fingerprint`, `engine_version` из рабочего дерева: `git diff HEAD` плюс untracked, SHA-256 по `Binaries/Linux/libUnrealEditor-GV2*.so`, `EngineAssociation` из `GV2.uproject`. Затем сверяется с идентичностью, которую рантайм выдал в отчёт (`GV2_RUNTIME_IDENTITY:`).

**Восемь путей возвращают sentinel вместо значения.** `unknown_revision`, `missing_binaries` (дважды), `unknown_uproject_missing`, `unknown_engine_association` и три вида `error:...`. Проверка `REQUIRED_IDENTITY_KEYS` требует непустую строку — все восемь ей удовлетворяют. То есть идентичность считается заполненной, когда она означает «определить не удалось».

**Discovered строится парсингом лога.** `parse_discovery_from_log` читает строки `LogAutomationCommandLine: Display:` фазы обнаружения и сверяет их число с объявленным движком (`Found N automation tests based on`); расхождение — исключение. Это единственный источник множества discovered.

## Почему дешёвые пути не дают результата

| Подход | Результат и цена | Выбор |
|---|---|---|
| Оставить как есть, прогон исполнять снаружи | `validate_run` вызывать нечем: три его входа живут в памяти чужого процесса | Не исполним |
| Передавать наружу только отчёт | Отчёт не содержит ни discovered, ни ожидаемой идентичности. Сверка «обнаружено = отчитано» исчезает, остаётся «отчёт непротиворечив сам себе» | Недостаточен |
| Доверять идентичности, которую прислал производитель | Производитель заверяет сам себя. При sentinel-значениях это ещё и заверение словом «не знаю» | Недостаточен |
| Выделить evidence bundle, закрыть sentinel, заставить потребителя самому вывести ревизию | Evidence переносимо, а доказательная сила не зависит от того, кто его собрал | Основа плана |

## Рекомендуемое устройство

### Evidence bundle как артефакт, а не как три переменные

Три входа `validate_run` становятся одним именованным артефактом с явной схемой: ожидаемая идентичность, множество discovered, нормализованный отчёт. Локальный раннер его пишет; валидация его читает. Кто исполнил прогон — деталь, а не условие проверки.

### Потребитель выводит ревизию сам

`source_revision` и `source_diff_hash` описывают **дерево, на котором проверяют**, а не прогон. Их обязан вывести потребитель из собственного репозитория и потребовать совпадения с bundle. Иначе evidence, собранное на другой ревизии, останется валидным — и это будет незаметно, потому что внутренне непротиворечиво.

`build_fingerprint` и `engine_version` описывают **исполнителя** и приходят из bundle: потребитель их не выводит, но обязан отвергнуть sentinel.

### Sentinel перестаёт быть значением

Заглушки собираются в именованное множество, и проверка идентичности отвергает принадлежность ему. Перечислитель — само множество плюс гейт, требующий, чтобы каждый fallback-возврат вычислителей идентичности возвращал его член: новая заглушка без регистрации проваливает гейт, а не проходит проверку молча.

### Нормативная форма запуска

argv приёмки перестаёт быть деталью раннера и становится нормативным: записан в contracts и выводится из одного места в коде. Без этого «серверный шаблон соответствует приёмке GV2» — утверждение, которое нечем проверить.

## Объём фиксации

План включает семь задач `AEP-01…07`. Фиксируемая поверхность — `Tools/Testing/run_ue_acceptance.py`, `Tools/Testing/ue_test_report.py`, их CTest-гейты и раздел приёмки в [Build and Tooling](../../Architecture/BuildAndTooling.md).

Вне плана: реализация `ue-build-service`, его registry и транспорт; решения `A`, `C`, `D`, `E` предложения; изменение множества automation test id.

## Этапы и зависимости

| Milestone | Задачи | Проверяемый результат |
|---|---|---|
| M0 — Evidence как артефакт | AEP-01…02 | Bundle со схемой, локальный раннер его пишет, валидация читает; неполный bundle — типизированный отказ |
| M1 — Идентичность перестаёт заверять себя | AEP-03…04 | Sentinel отвергается; потребитель сам выводит ревизию и требует совпадения |
| M2 — Форма запуска и эквивалентность | AEP-05…06 | argv нормативен и выводится из одного места; bundle двух производителей на одной ревизии совпадают |
| M3 — Закрытие | AEP-07 | Правила в contracts, открытый пункт предложения снят |

Зависимости: `01 → 02 → 04 → 05 → 06 → 07`, `03` параллельна и обязана предшествовать `06`.

`AEP-03` содержательно независима от bundle: sentinel — дефект нынешней проверки идентичности, существующий и без переноса. Она вынесена отдельно, чтобы не смешивать починку существующего с введением нового, и обязана быть закрыта до `AEP-06`, иначе сверка эквивалентности сравнивала бы в том числе заглушки.

- [x] M0 — AEP-01…02 приняты по Done/Evidence.
- [x] M1 — AEP-03…04 приняты по Done/Evidence.
- [ ] M2 — AEP-05…06 приняты по Done/Evidence.
- [ ] M3 — AEP-07 принят по Done/Evidence.

---

## M0 — Evidence как артефакт

### AEP-01 — Выделить evidence bundle с явной схемой

- [x] AEP-01 — Выделить evidence bundle с явной схемой

**Зависимость:** нет. **Файлы:** `Tools/Testing/ue_test_report.py` (схема и чтение), `Tools/Testing/run_ue_acceptance.py` (запись), `Tools/Testing/test_ue_test_report.py`.

**Инвариант:** [достоверная приёмка](../../Architecture/BuildAndTooling.md). Сегодня связь между evidence и породившим его прогоном обеспечена тем, что producer и consumer — один процесс. Это не свойство проверки, а свойство её нынешней упаковки: при любом разнесении оно исчезает, не оставив следа в коде.

**Решение:** три входа `validate_run` объединяются в артефакт с явной схемой — ожидаемая идентичность, множество discovered, нормализованный отчёт. Локальный раннер пишет его рядом с отчётом; валидация получает путь к bundle, а не значения из памяти. Первый производитель bundle — сам раннер, в этой же задаче.

**Не считается закрытием:** bundle без схемы, разбираемый «по факту»; писать bundle, но продолжать валидировать по внутренним значениям — тогда формат не проверяется ничем; класть в bundle отчёт по ссылке на путь, действительный только у производителя.

**Шаги:**
1. Определить схему bundle; версионировать её, чтобы несовпадение версии было отказом, а не попыткой разобрать.
2. Научить раннер писать bundle; отчёт и discovered складывать в него по значению.
3. Перевести валидацию на чтение bundle; прежний вызов по трём аргументам оставить только как внутреннюю деталь.
4. Прогнать приёмку локально: результат не изменился, bundle создан.

**Done:**
- Валидация исполняется по bundle; путь «значения из памяти» в production-коде отсутствует, перечислитель — вызывающие `validate_run`.
- Схема версионирована; несовпадение версии — типизированный отказ, случай покрыт негативным тестом.
- Отчёт и discovered лежат в bundle по значению; bundle пригоден к переносу на другую машину без файлов производителя.
- Локальная приёмка даёт тот же исход, что до change set.

**Evidence:** схема, прогон локальной приёмки с созданным bundle, негативный прогон на неверной версии схемы.

**Реализация.** Схема — `EVIDENCE_BUNDLE_SCHEMA_VERSION = "gv2-acceptance-evidence-bundle-v1"` (`Tools/Testing/ue_test_report.py`), версионирована отдельно от `SUPPORTED_REPORT_SCHEMA_VERSIONS` отчёта: bundle оборачивает отчёт, а не является им, и несовпадение версии bundle обязано отказывать раньше, чем вообще будет осмотрена версия вложенного отчёта. Три входа собираются `build_evidence_bundle(discovered, report, run_identity)` по значению — `discovered` сериализуется как отсортированный список (в JSON нет типа set), `report`/`run_identity` кладутся как есть, ссылок на путь производителя нет ни у одного поля.

`write_evidence_bundle`/`load_evidence_bundle` — запись и чтение с диска; `load_evidence_bundle` даёт `FileNotFoundError` на отсутствующий файл и типизированный `ValueError` на невалидный JSON, не-объект и несовпадение `bundle_schema_version` (сообщение называет обе версии — найденную и ожидаемую). `validate_evidence_bundle(bundle)` разворачивает by-value части и передаёт их тому же `validate_run`, который остаётся единственным местом, решающим pass/fail (шаг 3 плана: «прежний вызов по трём аргументам — внутренняя деталь»).

`run_ue_acceptance.py` заменил прямой вызов `validate_run(discovered, normalized_report, run_identity)` на `write_evidence_bundle` → `load_evidence_bundle` → `validate_evidence_bundle`: production-путь больше не валидирует по значениям из памяти, а перечитывает файл, который сам только что записал — тем самым бандл доказанно самодостаточен, а не просто одноимённая копия тех же переменных. Путь до bundle печатается в вывод раннера.

Перечислитель вызывающих `validate_run` (grep по `*.py`, вне тестов) — их два: `run_ue_acceptance.py` (переведён на bundle этой задачей) и `Tools/MCP/run_ue_tests.py`. Второй — не переведён и оставлен как есть: это интерактивный dev-loop раннер поверх уже запущенного редактора через MCP (producer и consumer всегда на одной машине по конструкции самого MCP-соединения), а не «приёмка» в смысле `ContainerizedExecutionEnvironmentProposal`, и не входит в объём фиксации плана (`Tools/Testing/run_ue_acceptance.py`, `Tools/Testing/ue_test_report.py`). Утверждение Done сужено до этой границы явно, а не расширено на файл вне списка задачи.

Подтверждено реальным прогоном: `RunUBT.sh` (чистая сборка), затем `python3 Tools/Testing/run_ue_acceptance.py --filter "GV2.UI.PreparedUiValue"` — свежий процесс `UnrealEditor-Cmd`, `evidence_bundle.json` реально создан в `Saved/Automation/Reports/`, содержит все три части по значению (проверено чтением файла). Валидация в этом прогоне корректно отказала на `source_diff_hash mismatch` — расхождение между ожидаемым (посчитан раннером перед запуском) и заявленным (встроен в бинарник на момент сборки) значениями; это не регрессия bundle-механизма (тот же диагностический путь, что и до change set, воспроизведён 12 юнит-тестами на управляемых данных с нулевым diagnostics на чистом входе), а именно то нестабильное поведение идентичности, которое `AEP-03`/`AEP-04` closes отдельно — здесь зафиксировано как наблюдение, не как находка этой задачи.

12 новых тестов в `test_ue_test_report.py` (`TestEvidenceBundle`): позитивный round-trip, переносимость bundle в каталог без файлов производителя, 6 негативных (отсутствующий файл, невалидный JSON, не-объект, несовпадение версии в `load_evidence_bundle` и в `validate_evidence_bundle`, отсутствие поля версии), сохранение диагностик вложенного `validate_run` через bundle, мутационная проба на незарегистрированную версию. `python3 Tools/Testing/test_ue_test_report.py`: 57/57 (было 45). Портативный ctest: `ue_test_report_contract`, `ue_acceptance_runner_contract`, `mcp_transport_contract` — 3/3 зелёные (`run_ue_acceptance.py --self-test` расширен тем же bundle round-trip и мутационной пробой на версию).

---

### AEP-02 — Сделать неполный bundle отказом

- [x] AEP-02 — Сделать неполный bundle отказом

**Зависимость:** AEP-01. **Файлы:** те же.

**Инвариант:** [достоверная приёмка](../../Architecture/BuildAndTooling.md). Отсутствующая часть evidence обязана останавливать приёмку, а не сокращать её. Пропуск части проверки, не отличимый от её прохождения, — тот же класс отказа, что гейт, прошедший потому, что никуда не посмотрел.

**Решение:** каждая обязательная часть bundle при отсутствии или нечитаемости даёт собственный типизированный отказ с указанием, чего не хватило. Умолчаний нет: пустое множество discovered — отказ, отсутствующий отчёт — отказ, отсутствующая идентичность — отказ.

**Не считается закрытием:** общий булев отказ на любую неполноту — он не отличает «отчёт не пришёл» от «прогон ничего не нашёл»; подстановка пустых значений по умолчанию; отказ только на верхнем уровне, без проверки обязательных полей внутри частей.

**Шаги:**
1. Перечислить обязательные части bundle и обязательные поля внутри них.
2. Реализовать типизированный отказ на каждую; на каждую — негативный тест.
3. Проверить мутацией: удаление любой одной части красит валидацию с названной причиной.

**Done:**
- Каждая обязательная часть имеет собственную причину отказа; перечислитель — список обязательных частей, а не просмотр кода.
- Удаление любой одной части красит валидацию; проверено мутационной пробой по всему перечню, а не на одном примере.
- Пустое множество discovered отвергается отдельно от отсутствующего.

**Evidence:** перечень обязательных частей, мутационные прогоны по каждой.

**Реализация.** Перечислитель — `REQUIRED_BUNDLE_PARTS = ("run_identity", "discovered", "report")` (`Tools/Testing/ue_test_report.py`), проверяемый в `validate_evidence_bundle` ДО обращения к содержимому любой части: отсутствующий ключ даёт `"Evidence bundle is missing required part '<part>'."` и останавливается на этом — подстановки умолчания нет. Часть, присутствующая, но пустая/неверного типа (пустой `discovered`, `report` не-dict, `run_identity` не-dict), проваливается отдельно — уже существующими проверками `validate_run` (и его же `REQUIRED_IDENTITY_KEYS` для полей внутри `run_identity`), которые сохранены без изменений и вызываются только после того, как top-level перечислитель подтвердил, что все три части на месте. Обе причины различимы текстом: «missing required part» против «Discovered test set is empty.» / «Report must be a dict...» / «Expected run_identity must be a dict...».

Мутационная проба прогнана по всему перечню (`test_mutation_each_required_part_removal_gives_named_diagnostic`, `subTest` на каждый из трёх элементов `REQUIRED_BUNDLE_PARTS` — не единственный пример): удаление любой одной части даёт ровно один диагностический элемент, называющий её по имени. Отдельный тест проверяет удаление сразу двух частей — обе названы, не только первая. Отдельный тест проверяет, что удаление `run_identity` не заменяется молчаливым дефолтом (не всплывают более глубокие диагностики `validate_run`, которые появились бы, будь дефолт подставлен). Пустое `discovered` (часть присутствует) даёт «Discovered test set is empty.» и явно НЕ даёт «missing required part» — assертами в одном тесте, различающими оба случая по обеим сторонам.

5 новых тестов в `test_ue_test_report.py`. `python3 Tools/Testing/test_ue_test_report.py`: 62/62 (было 57). Портативный ctest: `ue_test_report_contract`/`ue_acceptance_runner_contract`/`mcp_transport_contract` зелёные; полный `ctest` 134/134.

---

## M1 — Идентичность перестаёт заверять себя

### AEP-03 — Закрыть sentinel-значения идентичности

- [x] AEP-03 — Закрыть sentinel-значения идентичности

**Зависимость:** нет; обязана предшествовать AEP-06. **Файлы:** `Tools/Testing/ue_test_report.py`, `Tools/Testing/test_ue_test_report.py`.

**Инвариант:** [достоверная приёмка](../../Architecture/BuildAndTooling.md). Проверка `REQUIRED_IDENTITY_KEYS` требует непустую строку. Вычислители идентичности возвращают восемь заглушек — `unknown_revision`, `missing_binaries` (два пути), `unknown_uproject_missing`, `unknown_engine_association` и три вида `error:...`, — и каждая этому требованию удовлетворяет. Идентичность считается заполненной ровно тогда, когда означает «определить не удалось». С локальным производителем это маскируется тем, что рантайм выдаёт настоящее значение и сверка расходится; с удалённым производителем обе стороны могут не знать одного и того же.

**Решение:** заглушки собираются в именованное множество, и проверка идентичности отвергает принадлежность ему с указанием, какое поле не определено и почему. Множество является перечислителем: гейт требует, чтобы каждый fallback-возврат вычислителей идентичности возвращал его член, поэтому новая заглушка без регистрации проваливает гейт, а не проходит проверку.

**Не считается закрытием:** перечислить заглушки в проверке рукописным списком — он разойдётся с кодом при первой новой; заменить заглушки на пустую строку — это тот же отказ, выраженный тем же способом; отвергать только часть заглушек, оставив `error:...` как «тоже значение».

**Шаги:**
1. Свести заглушки в именованное множество; заменить литералы в вычислителях на его члены.
2. Отвергать принадлежность множеству в проверке идентичности с указанием поля.
3. Ввести гейт: каждый fallback-возврат вычислителей идентичности возвращает член множества.
4. Проверить мутацией: новая незарегистрированная заглушка проваливает гейт; зарегистрированная — проваливает проверку идентичности.

**Done:**
- Ни одно sentinel-значение не проходит проверку идентичности; перечислитель — само множество, не список в проверке.
- Новая заглушка без регистрации проваливает гейт; случай покрыт мутационной пробой.
- Каждый отказ называет поле, которое не определено, а не только факт отказа.
- Существующая приёмка проходит без изменений: на исправном дереве ни одна заглушка не возвращается.

**Evidence:** множество заглушек, вывод гейта, две мутационные пробы.

**Реализация.** Именованное множество — `SENTINEL_IDENTITY_VALUES` (5 точных литералов: `unknown_revision`, `unknown_diff`, `missing_binaries`, `unknown_uproject_missing`, `unknown_engine_association`) плюс `SENTINEL_IDENTITY_PREFIXES = ("error:",)` для трёх динамических вариантов, несущих текст пойманного исключения и потому не являющихся точным литералом. Предикат `is_sentinel_identity_value` заменил прежний `REJECTED_IDENTITY_PREFIXES` (широкие `"missing_"`/`"unknown_"`/`"no_"`/`"error:"`) — старые широкие префиксы были рукописной догадкой о форме будущих заглушек, а не отражением того, что код действительно возвращает; убраны в пользу точного множества, синхронизированного с реальными вычислителями гейтом.

Гейт — `validate_identity_sentinel_registration()`: парсит AST четырёх функций из `IDENTITY_SENTINEL_COMPUTERS` (`compute_source_revision`, `compute_source_diff_hash`, `compute_build_fingerprint`, `compute_engine_version`) через `extract_literal_string_returns`, извлекая каждый строковый литерал (и статичный ведущий сегмент f-строки — `"error:"` без интерполированной части) из их `return`. Настоящий успешный возврат в этих функциях — всегда вычисленное значение (`res.stdout.strip()`, `association.strip()`, hex-дайджест), поэтому любой литерал, найденный в возврате, — заглушка по построению, с одним исключением: `compute_source_diff_hash`'s собственное `"clean"` (нет diff) — легитимное успешное значение, а не отказ; исключено явно через `KNOWN_NON_SENTINEL_LITERAL_RETURNS`, а не угадыванием по форме. Новая заглушка без регистрации — незарегистрированный литерал — проваливает гейт с названием функции и самого литерала; тест `test_gate_is_clean_on_real_identity_computers` прогоняет гейт на реальных вычислителях в составе обычного прогона тестов, так что расхождение поймается при первом же прогоне, а не только при ручном вызове.

Обе мутационные пробы — на фейковых функциях той же формы (реальные вычислители мутировать на месте нельзя, не потеряв их собственный смысл): `test_mutation_unregistered_literal_return_is_detected` — фейковый вычислитель с двумя незарегистрированными литералами, оба найдены и помечены; `test_mutation_registering_prefix_covers_new_dynamic_literal` — новый `error:`-литерал с другим телом сообщения покрывается существующим префиксом без отдельной точной регистрации, доказывая, что префиксная половина множества действительно работает.

`validate_run` использует тот же `is_sentinel_identity_value` в обеих точках (ожидаемая идентичность и `run_identity` внутри отчёта) — одно место истины вместо двух объявлений префиксов, которые могли разойтись. Существующий тест `test_negative_rejected_identity_prefixes` переписан на перечислитель `SENTINEL_IDENTITY_VALUES` вместо рукописного списка (убрано `"no_git"` — оно проверяло префикс `"no_"`, для которого ни один вычислитель никогда не возвращал значения; сохранение отказа для несуществующей формы противоречило бы точности, которую вводит эта задача).

8 новых тестов (`TestIdentitySentinelRegistration`) плюс переписанный существующий. `test_ue_test_report.py`: 70/70 (было 62). Портативный ctest: `ue_test_report_contract`/`ue_acceptance_runner_contract`/`mcp_transport_contract` зелёные; полный `ctest` 134/134. `run_ue_acceptance.py` не менялся — гейт целиком внутри `ue_test_report.py`.

---

### AEP-04 — Потребитель сам выводит ревизию и требует совпадения

- [x] AEP-04 — Потребитель сам выводит ревизию и требует совпадения

**Зависимость:** AEP-02. **Файлы:** `Tools/Testing/ue_test_report.py`.

**Инвариант:** [достоверная приёмка](../../Architecture/BuildAndTooling.md). `source_revision` и `source_diff_hash` описывают дерево, на котором проверяют, а не прогон. Если их принимать из bundle, evidence, собранное на другой ревизии, останется валидным — и незаметно, потому что внутренне непротиворечиво: производитель заверяет сам себя.

**Решение:** потребитель выводит `source_revision` и `source_diff_hash` из собственного репозитория и требует совпадения с bundle; расхождение — отказ с указанием обеих величин. `build_fingerprint` и `engine_version` описывают исполнителя, поэтому приходят из bundle и потребителем не выводятся — к ним применяется только правило `AEP-03`.

**Не считается закрытием:** сверять ревизию, но принимать `source_diff_hash` из bundle — грязное дерево производителя останется незамеченным; выводить ревизию, но продолжать валидировать по значению из bundle; предупреждение вместо отказа.

**Шаги:**
1. Разделить поля идентичности на выводимые потребителем и принимаемые от исполнителя; зафиксировать разделение явно.
2. Выводить обе величины у потребителя и требовать совпадения.
3. Проверить прогоном: bundle с чужой ревизии отвергается; bundle с грязного дерева отвергается отдельной причиной.

**Done:**
- Поля разделены явно; принадлежность поля к группе не выводится из его имени, а записана.
- Bundle с ревизии, отличной от ревизии потребителя, отвергается; проверено подменой, а не рассуждением.
- Несовпадение `source_diff_hash` отвергается отдельной причиной от несовпадения ревизии.

**Evidence:** запись разделения полей, два прогона с подменённым bundle.

**Реализация.** Разделение записано явно, не выводится из имени поля: `CONSUMER_DERIVED_IDENTITY_KEYS = ("source_revision", "source_diff_hash")` (описывают проверяемое дерево — потребитель обязан вывести их сам), `EXECUTOR_PROVIDED_IDENTITY_KEYS = ("build_fingerprint", "engine_version")` (описывают исполнителя — потребитель вывести их не может, доверие остаётся правилу `AEP-03`); `run_id` намеренно не входит ни в одну группу (корреляционный id, а не свойство дерева/исполнителя). `assert`-проверка на месте объявления доказывает полноту разбиения: объединение обеих групп плюс `run_id` равно `REQUIRED_IDENTITY_KEYS` целиком, пересечение — пусто.

`derive_consumer_identity_overrides(repo_root=None)` вызывает `compute_source_revision`/`compute_source_diff_hash` над ЧЕКАУТОМ ПОТРЕБИТЕЛЯ (по умолчанию — репозиторий, в котором выполняется сам `ue_test_report.py`, как и `compute_run_identity`) и возвращает ровно два поля. `validate_evidence_bundle` больше не передаёт `bundle["run_identity"]` в `validate_run` как есть: копирует его и ЗАМЕЩАЕТ оба consumer-derived поля свежевычисленными — bundle-е собственное «ожидаемое» значение для `source_revision`/`source_diff_hash` перестаёт читаться вообще, не только перепроверяться. Значения executor-provided полей (`build_fingerprint`, `engine_version`) и `run_id` берутся из bundle без изменений, как раньше, — потребитель не может их вывести, и на них по-прежнему распространяется только правило `AEP-03`.

Оба прогона с подменённым bundle — юнит-тесты (`TestConsumerDerivedIdentity`), не рассуждение: `test_bundle_from_another_revision_is_rejected` подменяет `report.run_identity.source_revision` на чужой хэш при СОВПАДАЮЩЕМ `source_diff_hash` — отклонено с диагностикой, называющей `source_revision`; `test_bundle_from_dirty_tree_is_rejected_with_separate_reason_from_revision` подменяет только `source_diff_hash` — отклонено отдельной диагностикой, называющей `source_diff_hash`, и явно НЕ называющей `source_revision` (assert на отсутствие). Дополнительный тест (`test_bundle_expected_side_source_fields_are_ignored_not_trusted`) кладёт заведомую ложь в само bundle-е «ожидаемое» `run_identity` для обоих consumer-derived полей — валидация проходит чисто, потому что эти два поля из bundle вообще не читаются; `test_executor_provided_keys_still_come_from_bundle_not_consumer` подтверждает, что `build_fingerprint`/`engine_version` по-прежнему сверяются против bundle, а не изобретаются потребителем.

Существующие тесты AEP-01/AEP-02 (`TestEvidenceBundle`) monkeypatch'ат `compute_source_revision`/`compute_source_diff_hash` в `setUp`, чтобы их синтетическая идентичность не читалась как «evidence с чужой ревизии» из-за настоящего git-состояния этого репозитория — они проверяют механику bundle, не вывод идентичности, и не должны были зависеть от реального дерева. Самотест `run_ue_acceptance.py --self-test` обновлён аналогично: строит настоящий одноразовый git-репозиторий во ВРЕМЕННОЙ директории, ОТДЕЛЬНОЙ от директории с bundle-файлом (bundle рядом с репозиторием сделал бы сам себя новым untracked-файлом ко второму вычислению diff-хэша — найдено и исправлено при первом прогоне самотеста после этой правки), и передаёт её как `consumer_repo_root`.

7 новых тестов (`TestConsumerDerivedIdentity`), `test_ue_test_report.py`: 77/77 (было 70). Портативный ctest 134/134. Прогнан и реальный локальный `run_ue_acceptance.py` (без C++-пересборки, только Python-правка): `source_revision` совпал точно (подтверждает механизм); `source_diff_hash` разошёлся — это стало ожидаемым следствием того, что бинарник не пересобирался с последнего `RunUBT.sh` (его собственный diff-хэш «запечён» на момент прошлой сборки, а потребитель теперь честно выводит текущий), а не регрессией правки: расхождение дано ИМЕННО отдельной причиной от `source_revision`, как и требует `Done`.

---

## M2 — Форма запуска и эквивалентность

### AEP-05 — Сделать форму запуска приёмки нормативной

- [x] AEP-05 — Сделать форму запуска приёмки нормативной

**Зависимость:** AEP-04. **Файлы:** `Tools/Testing/run_ue_acceptance.py`, `Docs/Architecture/BuildAndTooling.md`.

**Инвариант:** [достоверная приёмка](../../Architecture/BuildAndTooling.md) и [универсальное утверждение требует перечислителя](../../Architecture/BuildAndTooling.md). Утверждение «серверный шаблон соответствует приёмке GV2» проверяемо только если форма запуска существует как проверяемый артефакт, а не как строка, собираемая по месту.

**Решение:** argv приёмки выводится из одного места в коде и записывается в contracts нормативно. Гейт сверяет запись в contracts с тем, что строит код: расхождение — отказ. Значимые элементы — `Automation RunTests` (не `RunTest`), явный `-abslog`, экспорт отчёта, `-unattended -nop4 -NullRHI`, семантика fresh-process.

**Не считается закрытием:** записать argv в contracts, не связав запись с кодом, — она разойдётся при первой правке; свести нормативность к перечислению флагов без порядка и значений, если они значимы; объявить нормативным то, что раннер собирает по условию.

**Шаги:**
1. Свести построение argv в одно место; сделать его доступным без запуска редактора.
2. Записать форму в contracts как нормативную.
3. Ввести гейт сверки contracts и кода; негативный self-test на расхождение.
4. Проверить, что локальная приёмка не изменилась.

**Done:**
- argv строится в одном месте и доступен без запуска редактора; перечислитель — вызывающие этого места.
- Contracts и код сверяются гейтом; расхождение красит, случай покрыт негативным self-test.
- Локальная приёмка даёт тот же исход, что до change set.

**Evidence:** запись в contracts, вывод гейта, негативный прогон.

**Реализация.** Единственное место построения — `ACCEPTANCE_ARGV_TEMPLATE` (кортеж литеральных токенов и `{placeholder}`-слотов) плюс чистая `build_acceptance_argv(editor_cmd, project_path, filter_expr, report_dir, log_path)` в `Tools/Testing/run_ue_acceptance.py`; `run_acceptance()` больше не собирает `cmd` инлайн, а вызывает эту функцию. Доступность без запуска редактора — двойная: функция сама по себе чистая и не имеет побочных эффектов (перечислитель — единственный production-вызывающий внутри `run_acceptance`, плюс гейт и CLI ниже), и добавлен `--print-argv` — печатает построенную команду и завершается без обращения к `UnrealEditor-Cmd`.

Нормативная запись — `Docs/Architecture/BuildAndTooling.md`, раздел «Нормативная форма запуска приёмки (AEP-05)»: тот же шаблон в ` ```text ` блоке, буквально, placeholder-в-placeholder. Гейт — `validate_acceptance_argv_contract(doc_path=None)`: находит заголовок раздела, берёт первый ` ```text ` блок после него (`extract_documented_acceptance_argv`, typed `ValueError` на отсутствующий заголовок/незакрытый блок — не `None`/пустой список), сравнивает построчно с `ACCEPTANCE_ARGV_TEMPLATE`; расхождение — диагностика с обеими сторонами. Убедиться, что гейт ищет блок именно после заголовка, а не первый `​```text` в файле вообще, потребовалось: в `BuildAndTooling.md` есть три других, не относящихся к AEP-05 `​```text` блока раньше по файлу — первая версия негативной пробы (мутация первого попавшегося блока во всём документе) этого не заметила и молча проверяла нетронутый реальный блок; исправлено срезом `real_doc_text` от индекса заголовка до мутации.

Три мутационные пробы (не одна): пропавшая строка внутри блока (`-nullrhi` вырезан) → «mismatch» с обеими сторонами; заголовок есть, `​```text` переименован в `​```plain` (блок пропал) → диагностика называет «fenced»; заголовок вырезан целиком → диагностика называет «heading». Все три — на временных копиях реального файла, не на синтетическом фикстурном тексте, так что мутация проверяет ИМЕННО реальную запись в contracts, а не выдуманный формат.

Гейт встроен в `run_ue_acceptance.py --self-test` (CTest `ue_acceptance_runner_contract`) — расхождение между кодом и contracts ловится тем же прогоном, что и остальные self-test проверки этого модуля, а не отдельным незапускаемым скриптом.

Локальная приёмка проверена дважды: до и после пересборки `RunUBT.sh` (Python-only задача, C++ не менялся). До пересборки — ожидаемое расхождение идентичности от несвежего бинарника (та же природа, что задокументирована в `AEP-04`); после пересборки — чистый `SUCCESS: All 1/1 discovered tests passed validation.`, exit 0, bundle создан — исход тот же, что и до этой задачи.

Портативный ctest 134/134; `validate_docs.py` — 195 файлов без ошибок.

---

### AEP-06 — Доказать эквивалентность двух производителей

- [ ] AEP-06 — Доказать эквивалентность двух производителей

**Зависимость:** AEP-05 и AEP-03. **Файлы:** тестовая обвязка в `Tools/Testing`.

**Инвариант:** [достоверная приёмка](../../Architecture/BuildAndTooling.md). Переносимость evidence — универсальное утверждение и требует перечислителя. Перечислитель здесь — два фактических bundle, а не рассуждение о том, что форма запуска одинакова.

**Решение:** на одной ревизии собираются два bundle: один локальным раннером, второй — исполнением нормативного argv из `AEP-05` напрямую, вне оркестрации раннера, с последующей сборкой bundle из его артефактов. Требуется совпадение множества automation test id, `build_fingerprint` и `engine_version`; `run_id` совпадать не обязан и в сравнение не входит.

**Не считается закрытием:** сравнить только число тестов; принять совпадение исхода вместо совпадения множеств; собрать второй bundle тем же раннером с другим флагом — это один производитель, а не два.

**Шаги:**
1. Собрать bundle локальным раннером.
2. Исполнить нормативный argv напрямую и собрать bundle из его артефактов.
3. Сравнить множества test id, `build_fingerprint`, `engine_version`; расхождение любой мощности блокирует задачу.
4. Прогнать валидацию на втором bundle: она проходит без ослаблений.

**Done:**
- Множества automation test id двух bundle равны; сверка множеств, не чисел.
- `build_fingerprint` и `engine_version` совпадают; `run_id` в сравнение не входит и это записано.
- Валидация проходит на bundle, собранном вне раннера, без изменения правил.
- Второй производитель не использует оркестрацию раннера; перечислитель — его собственный код, а не флаг раннера.

**Evidence:** два bundle, diff множеств test id, прогон валидации на втором bundle.

---

## M3 — Закрытие

### AEP-07 — Перенести правила в contracts и снять открытый пункт

- [ ] AEP-07 — Перенести правила в contracts и снять открытый пункт

**Зависимость:** AEP-06. **Файлы:** `Docs/Architecture/BuildAndTooling.md`, `Docs/Proposals/ContainerizedExecutionEnvironmentProposal.md`.

**Инвариант:** правило индекса планов: полностью выполненный план перестаёт быть источником задач, а его нормативный результат к этому моменту обязан быть перенесён в contracts.

**Решение:** нормативными становятся форма bundle, разделение полей идентичности на выводимые потребителем и принимаемые от исполнителя, запрет sentinel и форма запуска. В предложении снимается открытый пункт про источник приёмочного evidence. Остаток, если он есть, выносится строкой `STATUS-NNN` — следующий свободный номер в момент записи.

**Не считается закрытием:** закрыть план, оставив в предложении открытым пункт, который он закрывает; перенести форму bundle, не перенеся разделение полей — без него форма не защищает; занять номер `STATUS-NNN` заранее.

**Шаги:**
1. Перенести четыре правила в contracts как нормативные.
2. Снять открытый пункт в предложении; остаток вынести строкой.
3. Полный портативный CTest и полная UE-приёмка.
4. Отметить milestones и заархивировать план по правилам [индекса](../README.md).

**Done:**
- Четыре правила перенесены и на них ссылается реализация.
- Открытый пункт предложения снят; остаток, если есть, имеет строку `STATUS-NNN`.
- Полный CTest и полная UE-приёмка зелёные на ревизии закрытия.

**Evidence:** diff contracts, diff предложения, полные прогоны.

## Правила выполнения и остановки

- Change set не объединяет введение механизма и его первого потребителя: механизм доказывается негативными тестами, потребитель — поведением.
- Задача, потребовавшая изменить архитектурный инвариант, останавливается до ADR.
- Evidence задач M2 требует прогона UE automation, конфликтующего с живым редактором; они выполняются в сессии, где это учтено.
- Реализация `ue-build-service` планом не покрывается: план доводит сторону GV2 до состояния, в котором серверный шаблон проверяем, и не делает предположений о его внутреннем устройстве.

## Критерий выхода

Приёмочное evidence существует как переносимый артефакт с версионированной схемой; неполнота любой его части — типизированный отказ. Ни одно sentinel-значение не проходит проверку идентичности, и новая заглушка без регистрации проваливает гейт. `source_revision` и `source_diff_hash` выводит потребитель и требует совпадения; `build_fingerprint` и `engine_version` приходят от исполнителя. Форма запуска нормативна и сверяется гейтом с кодом. Bundle, собранный вне раннера исполнением нормативного argv, проходит ту же валидацию и даёт то же множество automation test id.
