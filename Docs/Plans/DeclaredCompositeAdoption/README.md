---
title: Declared Composite Adoption and Enumerator Hardening Plan
status: active
version: 3.0
updated: 2026-09-06
depends_on:
  - ../Archive/DataDrivenUiComposition.md
  - ../Archive/GenericBoundaryHardening.md
  - ../../UI/ScreenTemplates.md
  - ../../UI/WidgetRegistry.md
  - ../../Status/ImplementationStatus.md
  - ../../Status/GV2_CommandPanelWrapSizeFixedNonScaling_2026-09-03.md
decisions:
  - ../../ADR/0040-universal-ui-property-pipeline.md
  - ../../ADR/0035-ui-foundation-and-composition.md
---

# План перевода композитов на объявление и устранения рукописных перечислителей

> **Материализует:** исправленную запись о трёх композитах локации в [DataDrivenUiComposition](../Archive/DataDrivenUiComposition.md) и требование собирать экран в Designer из любых доступных компонентов.
> **Задачи:** DCA-01…21.
> **Результат:** в коде не остаётся ни одного C++-класса виджета, существующего только ради композиции существующих элементов, ни одного специального мостика под конкретный композит и ни одного множества, перечисленного руками там, где перечислитель уже существует.

## Цель

Отчёт `DUC-08` записал, что три композита локации к объявлению не сводятся. Проверка это опровергла: набор capability всех трёх выражается объявлением полностью, а названная причина по `CommandPanel` — `OnBindingInvoked` — оказалась мёртвой поверхностью без единого `Broadcast` и без подписчиков.

Настоящее препятствие одно и лежит в ассетах: композит принимает голую панель и создаёт репитер внутри себя в `NativePreConstruct`. Этот объект — transient `UPROPERTY`, его нет в `WidgetTree`, поэтому адресация по имени до него не дотягивается. Ради обхода в `GV2UiMutationPlan.cpp` живёт хардкод трёх `Cast` по конкретным классам локации с перечислением имён контейнеров.

**Главный измеримый результат плана — исчезновение этого мостика.** Пока он существует, утверждение «модель покрывает любой композит» имеет живое исключение, и разговор о будущих компонентах опирается на модель с дырой.

Вторая половина плана — доказательство с другой стороны: три **полностью новых** композита, собранных без единой строки C++. Миграция доказывает, что старое выражается объявлением; авторинг — что новое не требует кода.

## Общий инвариант

`M1…M3` были планом про композиты. `M4` и `M5` пришли из сплошной проверки проекта, которую спровоцировал разбор `WrapSize=1200`, и оказались тем же самым: **множество, которое должен перечислять механизм, перечислено руками**. Это и есть то, что план закрывает.

Проверка дала наблюдение, которое стоит держать перед глазами при выполнении любой задачи отсюда: во всех найденных случаях авторитетный источник **уже существует в проекте и не читается**.

| Что перечислено руками | Источник, который уже есть | Задача |
|---|---|---|
| Адресуемость через внутренний репитер | `WidgetTree` | DCA-02 ✔ |
| Класс элемента и путь ассета в C++ | Запись объявления | DCA-03 ✔ |
| Разрешение цели по трём `Cast` | Реализации `IGV2ScreenFieldHost`/`IGV2UiPropertyHost` | DCA-08 ✔ |
| Порог переноса и абсолютные размеры | Фактически выделенная геометрия | DCA-15 ✔ |
| Контракт компонентов по массиву из 17 | Asset registry, который тот же тест уже обходит | DCA-17 ✔ |
| Порядок слоёв и владелец схемы | `load_index` в `GameData/mods.lock.json5` | DCA-18…20 |

Отсюда практическое правило выполнения: прежде чем писать список, назвать источник, который его заменяет. Если источника действительно нет — это и есть содержание задачи, а не повод писать список.

Обратная сторона того же наблюдения — `M4`: там, где перечислитель отсутствует, проверка со временем подстраивается под инструмент, а не под продукт, и компромисс уходит в артефакт молча.

## Состояние на входе

| Что | Сейчас |
|---|---|
| Хосты коллекций в трёх композитах | Голые `UWrapBox`/`UPanelWidget`; репитер создаётся внутри и отсутствует в `WidgetTree` — **закрыто DCA-02, 2026-09-03** |
| `GV2UiMutationPlan.cpp` | Ни одного `Cast` по классу локации не осталось; отсутствие подтверждено runtime-гейтом (по образцу `GBH-05`) с отрицательным самотестом и проверкой уникальности логики разрешения цели — **закрыто DCA-08, 2026-09-03** |
| Запись объявления | Не имеет признака необязательности; композиты объявляют capability условно, `if (Target != nullptr)` — **закрыто DCA-01, 2026-09-03** |
| `Resolve*WidgetClass` | Удалены (все шесть); трёхступенчатый откат снят — **закрыто DCA-03, 2026-09-03** |
| `OnBindingInvoked` у `CommandPanel` | Удалён вместе с делегатом `FGV2LocationCommandBindingInvoked` — **закрыто DCA-04, 2026-09-03** |
| `StaminaMeter`, `Character` | Удалены; `StaminaMeter` также был реально привязан в `WBP_PlayerStatusPanel`, отвязка через `unreal-mcp` — **закрыто DCA-04, 2026-09-03** |
| Новые композиты из данных | Ни одного; объявляемый композит доказан только на `TopBar` и синтетических фикстурах |
| Отзывчивость раскладки | `ADR-0035` не имеет перечислителя; `WrapSize=1200` в `WBP_ListView_WrapButtons` — замер с 720p, вмороженный как константа |
| Гейты и `Content/` | Ни один гейт не читает ассеты: все семь — текстовые сканы по `Source/` и `Docs/` |
| Граница пакетов | `rh:`/`sample:` в `Source/` отсутствуют, но порядок слоёв, корни схем и владелец схемы закодированы литералами в трёх файлах и расходятся с `mods.lock.json5` |

## Принятые решения

- **Мостик удаляется, а не обобщается.** Соблазн — сделать поиск внутренних репитеров общим для любого хоста. Это закрепило бы адресацию объекта вне `WidgetTree` как часть модели; вместо этого ассеты приводятся к тому, что модель уже умеет.
- **Признак необязательности принадлежит объявлению, а не виджету.** Сегодня условность выражена в C++ (`if (Target != nullptr)`), и именно она мешает выразить варианты одного композита. Перенос её в запись объявления — необходимое условие миграции, а не удобство.
- **Доказательство авторинга измеримо.** Задачи трёх новых композитов закрываются в том числе тем, что их change set **не содержит изменений в `Source/`**. Утверждение «C++ не понадобился» проверяется diff-ом, а не памятью исполнителя.
- **Разбор `WrapSize` не сворачивается в правку одного значения.** `DCA-02` перенёс константу как есть — по своему скоупу он обязан был сохранить поведение. Но её происхождение (замер, снятый на единственном строго проверяемом разрешении, и вмороженный в ассет) означает, что чинить надо инструмент и перечислитель, а не число. Отсюда `M4`.
- **Новые композиты выбраны лесенкой.** `npc_portrait` — два листа без коллекций; `location_description` — необязательное свойство в реальном случае; `inventory_tabs` — полная цепочка `экран → вкладки → блок → коллекция` из данных, то есть самое глубокое, что модель обещает.

## Границы

Не входит: реестр адаптеров для голых UMG-контролов, недостижимых сегодняшними consumers (`USlider`, `USpinBox` и прочие). Это следующий шаг к «любым будущим компонентам», и он опирается на результат этого плана, а не наоборот. Также не входят `STATUS-001`, `STATUS-002`, `STATUS-003`, `STATUS-008`.

## Milestones

- [x] M1 — [Prerequisites](Prerequisites.md): объявление умеет всё, что умеют три композита; ассеты приведены. DCA-01…04. *(закрыт целиком 2026-09-03)*
- [x] M2 — [Migration](Migration.md): три композита переведены, классы и мостик удалены. DCA-05…08. *(закрыт целиком 2026-09-04)*
- [x] M3 — [Authoring](Authoring.md): три новых композита собраны из данных. DCA-09…12. *(закрыт целиком 2026-09-04)*
- [x] M4 — [Layout Invariant](LayoutInvariant.md): у требования отзывчивой раскладки появляется перечислитель. DCA-13…17. *(закрыт целиком 2026-09-04)*
- [ ] M5 — [Package Boundary](PackageBoundary.md): принадлежность к пакету и порядок слоёв выводятся из замыкания. DCA-18…21.

## Критический путь

```text
DCA-01✔, DCA-02✔, DCA-03✔, DCA-04✔   — независимы друг от друга

           └──────► DCA-05✔, DCA-06✔, DCA-07✔ ──────► DCA-08✔ ──────► DCA-09✔, DCA-10✔, DCA-11✔ ──► DCA-12✔

DCA-05…07 переставляются между собой

DCA-13✔ ──► DCA-15✔        DCA-14✔, DCA-16✔, DCA-17✔ — независимы

DCA-18✔, DCA-19, DCA-20 — независимы ──────► DCA-21

M4 (DCA-13…17) и M5 (DCA-18…21) не зависят от M1…M3 и переставляются свободно
```

`M4` возник из разбора `WrapSize=1200`, найденного при `DCA-02`; `M5` — из той же проверки, дошедшей до границы пакетов. Ни один не блокирует миграцию и не блокируется ею.

| Файл | Владеет |
|---|---|
| `GV2RuntimeSubsystemTests.cpp` | `M4` (DCA-13, DCA-14, DCA-17) и `DCA-18` — разные функции, разные утверждения |
| `WBP_ListView_Wrap*` | `M4`; `M2` и `M3` их не трогают |
| `GV2UiCapability.cpp` | `DCA-20` (`IsModNamespace`); `DCA-08` работает в `GV2UiMutationPlan.cpp` |
| `GV2ScreenRegistry.cpp`, `GV2ScreenFieldMaterializer.cpp` | только `M5` |

## Общие правила выполнения

1. Задача не отмечается `[x]`, пока не назван гейт, который **покраснеет** при откате изменения, и это не продемонстрировано.
2. Ассеты меняются только через `unreal-mcp`, с compile, save и прогоном automation.
3. Удаляемая поверхность удаляется, а не помечается устаревшей: `DeprecatedProperty` в этом плане исходом не является.
4. Каждый новый объявляемый композит попадает в sweep наблюдаемости тем же change set, что и его объявление.
5. Правки нормативных контрактов выполняются в той же задаче, что и код или ассет.

## Итоговый Definition of Done

- [x] Запись объявления выражает необязательное свойство; ссылка на непривязанного ребёнка отклоняется, а объявленная необязательной — нет. (DCA-01)
- [x] Ни один композит локации не создаёт репитер вне `WidgetTree`. (DCA-02)
- [x] Класс элемента каждой коллекции задан явно; трёхступенчатый откат `Resolve*WidgetClass` удалён; литерала `/Game/` в production-коде не остаётся, и это утверждает гейт. (DCA-03)
- [x] `OnBindingInvoked`, `StaminaMeter` и `Character` физически отсутствуют. (DCA-04)
- [x] `UGV2LocationSceneWidgetBase`, `UGV2LocationPlayerStatusWidgetBase` и `UGV2LocationCommandPanelWidgetBase` удалены из исходников. (DCA-05…07, 2026-09-03)
- [x] Хардкод `Cast` по классам локации в `GV2UiMutationPlan.cpp` удалён; для трёх удалённых классов возврат невозможен по построению — типов не существует, — а для будущих его не даёт гейт с перечислителем по интерфейсу хоста. (DCA-08, 2026-09-03)
- [x] Экран локации работает без изменения поведения, проверено сквозным прогоном от Lua до наблюдаемого состояния виджетов; объём проверки — пересечение объявления и зафиксированной схемы плюс отрисованный текст листа. (DCA-05…08, 2026-09-04)
- [x] Существуют три новых композита — `npc_portrait`, `location_description`, `inventory_tabs` — собранные из существующих компонентов. (DCA-09…11, 2026-09-04)
- [x] Change set каждого из трёх не содержит изменений в production-коде `Source/`, проверено независимым `git diff --stat` каждого коммита; DCA-11's единственный тестовый файл разобран отдельно. (DCA-12, 2026-09-04)
- [x] `inventory_tabs` доказывает цепочку `экран → вкладки → блок → коллекция` целиком из данных, включая отказ на нижнем уровне. (DCA-11, 2026-09-04)
- [x] Матрица разрешений проверяет геометрию кнопок строго на всех шести разрешениях, а фактическое покрытие видно в выводе теста. (DCA-13, 2026-09-04)
- [x] Отрицательные проверки `BAI-10` испытывают тот же предикат, который выносит положительный вердикт. (DCA-14, 2026-09-04)
- [x] Ни один параметр раскладки production-экранов не является константой без записанной причины, и это утверждает перечислитель по дереву виджетов — первый гейт проекта, читающий `Content/`. (DCA-15, 2026-09-04)
- [x] Правило о направлении обоснования между проверкой и артефактом записано в `AGENTS.md` и `Docs/Plans/README.md`. (DCA-16, 2026-09-04)
- [x] Аудит UI-компонентов проверяет контракт у каждого ассета, который сам же перечисляет; признак «база Text Pipeline» — свойство класса, а не список из двенадцати имён. (DCA-17, 2026-09-04)
- [x] Правило слоёв выводится из порядка пакетов замыкания и отклоняет namespace, которого в замыкании нет. (DCA-18, 2026-09-06)
- [ ] Ни один список корней схем не живёт отдельно от пиннингованного замыкания. (DCA-19)
- [ ] Политика отказа по владельцу схемы наблюдаема в поведении либо отсутствует вместе со своей поверхностью. (DCA-20)
- [ ] Имя пакета выше `core` не встречается в production-коде `Source/`, и это утверждает гейт. (DCA-21)
